# Constants Files

### Overview

The files in the `Constants` folder are values used throughout the entirety of the codebase. The most important ones are the `TransmitterConstants` and `ControlConstants` which are used for values of the pins, the communication baudrates, the behavioral constants, and some other key values.
